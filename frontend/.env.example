# URL of the deployed FastAPI backend (no trailing slash), e.g.
# https://hcp-crm-backend.onrender.com
# Leave empty for local dev (Vite proxy in vite.config.js handles /api calls).
VITE_API_BASE_URL=
